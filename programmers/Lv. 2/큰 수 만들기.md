### 처음 문제를 보고 생각한 자료구조 및 알고리즘

그리디

### 사용된 자료구조 및 알고리즘

그리디

### 걸린 시간

fail

### 나의 풀이

```java
import java.util.*;

class Solution {
    public String solution(String number, int k) {
        StringBuilder answer = new StringBuilder();
        for(int i=0;i<number.length();i++){
            char current = number.charAt(i);
            
            while(k>0&&answer.length()>0&&answer.charAt(answer.length()-1)<current){
                answer.deleteCharAt(answer.length()-1);
                k--;
            }
            
            answer.append(current);
        }
        if(k>0) answer.setLength(answer.length()-k);
        
        return answer.toString();
    }
}
```

처음에 정렬해서 답을 구해서 위치도 바뀐 최댓값을 구했었는데, 위치를 지키면서 최댓값을 구하는 거였다. 앞의 숫자와 비교해서 나보다 작으면 k개까지 지우는 식으로 답을 구했다. 만약 내림차순으로 되어있으면 마지막에서 k개를 순서로 끊었다.